CREATE TABLE "$tableName" (
#foreach ($column in $columns)
"$column.columnName" $column.columnType #if($column.isNull) NOT NULL #if($column.columnDefault) DEFAULT $column.columnDefault #end #end #if($column.isIncrement) GENERATED BY DEFAULT AS IDENTITY #end#if($foreach.hasNext),#end
#end
#if($!primaryKeyColumnName != "")
,PRIMARY KEY ("$primaryKeyColumnName")
#end
);

COMMENT ON TABLE "$tableName" IS '$tableComment';
#foreach ($column in $columns)
COMMENT ON COLUMN "$tableName"."$column.columnName" IS '$column.columnComment';
#end
