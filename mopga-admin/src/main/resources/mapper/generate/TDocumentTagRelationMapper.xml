<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gklx.mopga.admin.module.generate.dao.TDocumentTagRelationDao">

    <!-- 查询结果列 -->
    <sql id="base_columns">
        t_document_tag_relation.id,
        t_document_tag_relation.document_id,
        t_document_tag_relation.tag_id
    </sql>

    <!-- 分页查询 -->
    <select id="queryPage" resultType="com.gklx.mopga.admin.module.generate.domain.vo.TDocumentTagRelationVo">
        SELECT
        <include refid="base_columns"/>
        FROM t_document_tag_relation
    </select>
</mapper>