<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gklx.mopga.admin.module.generate.dao.DatabaseTermDao">

    <!-- 查询结果列 -->
    <sql id="base_columns">
        ai_database_term.id,
        ai_database_term.create_user_id,
        ai_database_term.create_time,
        ai_database_term.update_user_id,
        ai_database_term.update_time,
        ai_database_term.deleted_flag,
        ai_database_term.terms,
        ai_database_term.database_id,
        ai_database_term.database_comment_term
    </sql>

    <!-- 分页查询 -->
    <select id="queryPage" resultType="com.gklx.mopga.admin.module.generate.domain.vo.DatabaseTermVo">
        SELECT
        <include refid="base_columns"/>
        FROM ai_database_term
        <where>
            <if test="queryForm.databaseId != null">
                AND ai_database_term.database_id = #{queryForm.databaseId}
            </if>
        </where>
	    ORDER BY ai_database_term.update_time
    </select>
</mapper>