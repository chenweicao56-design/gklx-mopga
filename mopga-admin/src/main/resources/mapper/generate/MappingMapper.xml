<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gklx.mopga.admin.module.generate.dao.MappingDao">

    <!-- 查询结果列 -->
    <sql id="base_columns">
        gen_mapping.id,
        gen_mapping.name,
        gen_mapping.code,
        gen_mapping.remark,
        gen_mapping.sort,
        gen_mapping.create_user_id,
        gen_mapping.create_time,
        gen_mapping.update_user_id,
        gen_mapping.update_time,
        gen_mapping.deleted_flag
    </sql>

    <!-- 分页查询 -->
    <select id="queryPage" resultType="com.gklx.mopga.admin.module.generate.domain.vo.MappingVo">
        SELECT
        <include refid="base_columns"/>
        FROM gen_mapping
        <where>
            <if test="queryForm.name != null and queryForm.name != ''">
                AND gen_mapping.name like concat('%', #{queryColumn.fieldName}, '%')
            </if>
            <if test="queryForm.code != null">
                AND gen_mapping.code = #{queryForm.code}
            </if>
        </where>
        ORDER BY gen_mapping.sort ASC
    </select>
</mapper>
