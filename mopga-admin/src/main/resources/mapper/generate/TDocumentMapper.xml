<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gklx.mopga.admin.module.generate.dao.TDocumentDao">

    <!-- 查询结果列 -->
    <sql id="base_columns">
        t_document.id,
        t_document.document_name,
        t_document.document_content,
        t_document.file_type,
        t_document.document_type,
        t_document.version,
        t_document.create_by,
        t_document.create_time,
        t_document.update_by,
        t_document.update_time
    </sql>

    <!-- 分页查询 -->
    <select id="queryPage" resultType="com.gklx.mopga.admin.module.generate.domain.vo.TDocumentVo">
        SELECT
        <include refid="base_columns"/>
        FROM t_document
	    ORDER BY t_document.update_time
    </select>
</mapper>