# 文档管理系统 - 领域模型

## 核心概念

### 文档 (Document)

**定义**：一个文档代表一个可管理的文件实体，包含元数据和内容。

**组成**：
- **元数据**：文档名称、文件类型、文档类型、版本号、创建人、创建时间等
- **内容**：文件内容（直接存储在数据库中）
- **标签**：多个标签，用于分类和过滤

**关键属性**：
- 文档名称：文档的标识名称
- 文档内容：文件的实际内容（文本/代码/JSON/Markdown等）
- 文件类型：字典值（如：markdown、json、text、code）
- 文档类型：字典值（如：技术文档、用户手册、配置文件）
- 版本号：用户手动指定的版本标识
- 标签：多对多关系

**业务规则**：
- 只有创建人可以编辑或删除文档
- 每个文档可以有多个历史版本
- 历史版本不可编辑，只能查看或删除

### 版本 (Version)

**定义**：文档在某个时间点的快照。

**关键属性**：
- 版本号：用户指定的版本标识
- 文档内容：快照时的内容
- 创建人：创建该版本的用户
- 创建时间：版本创建时间

**业务规则**：
- 同一个文档可以有多个版本
- 历史版本不可编辑
- 可以删除历史版本（硬删除）

### 标签 (Tag)

**定义**：用于分类和过滤文档的关键词。

**关键属性**：
- 标签名：标签的名称
- 创建人：创建该标签的用户

**业务规则**：
- 一个文档可以对应多个标签
- 标签可以是预设的，也可以是用户自定义的
- 预设标签通过字典表维护

### 字典 (Dictionary)

**定义**：系统预设的枚举数据，用于维护可管理的选项列表。

**组成**：
- **字典定义**：字典名称、字典编码
- **字典数据**：字典项的值和显示名称

**用途**：
- 文件类型字典：定义支持的文件类型列表
- 文档类型字典：定义文档分类列表
- 预设标签字典：定义系统预设的标签列表

## 实体关系

```
Document (文档)
├── has many → DocumentTag (文档标签)
│   └── refers to → Tag (标签)
├── has many → DocumentVersion (文档版本)
│   ├── created by → User (创建人)
│   └── contains → Document content
└── created by → User (创建人)

Dict (字典定义)
├── has many → DictData (字典数据)
│   └── used for → File Type / Document Type / Predefined Tags
```

## 查询场景

1. **按标签过滤文档**：查询包含特定标签的所有文档
2. **按文件名称搜索**：模糊匹配文档名称
3. **按文档类型过滤**：查询特定文档类型的文档
4. **按文件类型过滤**：查询特定文件类型的文档
5. **查看历史版本**：查询某个文档的所有版本
6. **版本追溯**：查看某个版本的详细信息

## 审计字段

所有核心实体包含：
- 创建人 (create_user_id)
- 创建时间 (create_time)
- 更新人 (update_user_id)
- 更新时间 (update_time)
